{
  "name": "chatbot-project",
  "version": "1.0.0",
  "description": "Fictional Chatbot powered by Jeff",
  "scripts": {
    "dev": "vercel dev",  // Local testing
    "build": "vercel build" // Build for deployment
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  },
  "engines": {
    "node": "14.x"
  }
}
